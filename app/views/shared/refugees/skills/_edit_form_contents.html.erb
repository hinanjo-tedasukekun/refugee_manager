<p>以下のうち、得意な分野があればチェックを入れてください。</p>
<div class="well">
  <%= f.collection_check_boxes :skill_ids, Skill.all, :id, :name do |b| %>
    <div class="checkbox">
      <%= b.label { b.check_box + ' ' + b.text } %>
    </div>
  <% end %>
</div>
<%= f.submit '更新', class: 'btn btn-primary btn-block' %>
