<p>以下のうち、食べられないものがあればチェックを入れてください。</p>
<div class="well">
  <%= f.collection_check_boxes :allergen_ids, Allergen.all, :id, :name do |b| %>
    <div class="checkbox">
      <%= b.label { b.check_box + ' ' + b.text } %>
    </div>
  <% end %>
</div>
<div class="form-group">
  <p>その他に食べられないものがある場合、以下に記入してください。</p>
  <%= f.text_field :other_allergens, class: 'form-control' %>
</div>
<%= f.submit '更新', class: 'btn btn-primary btn-block' %>
