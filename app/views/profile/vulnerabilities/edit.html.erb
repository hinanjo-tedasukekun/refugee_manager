<% provide(:title, '要配慮事項') %>
<ol class="breadcrumb">
  <li><%= link_to 'ホーム', root_path %></li>
  <li><%= link_to '個人情報', profile_path %></li>
  <li class="active">要配慮事項</li>
</ol>
<%= render 'shared/flash' %>
<h1>要配慮事項</h1>
<%= form_for(@refugee, url: profile_vulnerabilities_path) do |f| %>
  <%= render 'shared/error_messages', target: @refugee %>
  <p>以下のうち、該当する事項にチェックを入れてください。</p>
  <div class="well">
    <%= f.collection_check_boxes :vulnerability_ids, Vulnerability.all, :id, :name do |b| %>
      <div class="checkbox">
        <%= b.label { b.check_box + ' ' + b.text } %>
      </div>
    <% end %>
  </div>
  <%= f.submit '更新', class: 'btn btn-primary btn-block' %>
<% end %>
